// src/components/booking/SectionHeader.tsx
const SectionHeader = ({ title }: { title: string }) => (
  <h2 className="text-lg font-bold text-gray-800 my-4">{title}</h2>
);
export default SectionHeader;